import styled from "styled-components";

export const TabPosition = styled.div`
    position: relative;
    justify-content: center;
    margin: auto;
    display: flex;
    background-image: url(${props=>props.bgfilter});
    background-repeat: no-repeat;
    background-position: center;

`